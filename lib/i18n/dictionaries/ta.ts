// Tamil dictionary — v2 Sarvam lane fills this (T7). Partial by design:
// missing keys fall back to English. Translate UI copy by hand or via
// lib/translate; review before shipping — UI chrome is short-form text where
// machine output needs a human eye.
export const ta: Record<string, string> = {};
