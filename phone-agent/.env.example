# Copy to .env and fill in. Never commit the real .env (see .gitignore).

SARVAM_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Optional overrides (defaults live in agent.py):
# QUOTRA_PHONE_LLM_MODEL=sarvam-105b
# QUOTRA_PHONE_TTS_LANGUAGE=en-IN
# QUOTRA_PHONE_TTS_VOICE=anushka
